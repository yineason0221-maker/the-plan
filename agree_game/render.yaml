services:
  - type: web
    name: agree-game
    env: python
    plan: free
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python server.py
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_PASSWORD
        sync: false
