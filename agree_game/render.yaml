services:
  - type: web
    name: agree-game
    env: python
    plan: free
    buildCommand: echo "No dependencies to install"
    startCommand: python server.py
    rootDirectory: agree_game
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_PASSWORD
        sync: false
